{# src/OC/PlatformBundle/Resources/views/Advert/add.html.twig #}

{% extends "OCPlatformBundle::layout.html.twig" %}

{% block body %}

  <h2>Ajouter une annonce</h2>

  {{ include("OCPlatformBundle:Advert:form.html.twig") }}

  <p>
    Attentions : cette annonce sera ajoutée directement
    sur la page d'accueil après validation du formulaire.
  </p>
  {# On affiche tous les messages flash dont le nom est « notices » #}
 {% for message in app.session.flashbag.get('notices') %}
     <p> class="alert alert-info">Message flash : {{ message }}</p>>
 {% endfor %}
 

{% endblock %}